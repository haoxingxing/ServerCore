# EXAM.LOGIN
echo("Please set the password:"); 
input(password);
var(is_loggout,true);
var(max_err,5);
var(err_count,0);
while(is_loggout.and(err_count.less(max_err)));
echo("Please input password:");
input(str);
if(str.equals(password));
echo("Success\n");
var(is_loggout,false);
else;
echo("Error\n");
var(err_count,err_count.plus(1));
echo("You have ",max_err.minus(err_count)," times left to try\n");
end;
end;
if(err_count.equals(max_err));
echo("You account is disabled\n");
end;