name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: install cmake
      run: |
        apt update
        apt install cmake
    - name: cmake
      run: cmake .
    - name: make
      run: make
