# Root Class
本语言的实现使用了C++的继承和多态性  
此根类即为 "万物为类" 的实现  
所有的子类都必须从此类派生

此类必须提供几个 __接口__
- __构造__
- __析构__
- __拷贝__
- __转换到__

子类化是需提供一个动态字符串(运行时)  
每一个类都自建 _定义域_  可以使用 Func access_member来访问  





*[Back](README.md)*