language: cpp
os:
  - linux
  - osx
script: 
  - cmake . && make
  - build/ServerCore*
  - build/ServerCore* tests.starcore

deploy:
  provider: releases
  api_key: 
    secure: $key
  file_glob: true
  file: build/*
  skip_cleanup: true
  on:
    tags: true