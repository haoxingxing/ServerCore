#!./build/StarCore*
# Test File
#var(password,"123456");
#var(is_loggout,true);
#var(max_err,3);
#var(err_count,0);
#while(is_loggout.and(err_count.less(max_err)));
#echo("Please input password:");
#input(str);
#if(str.equals(password));
#echo("Success\n");
#var(is_loggout,false);
#else;
#echo("Error\n");
#var(err_count,err_count.plus(1));
#echo("You have ",max_err.minus(err_count)," times left\n");
#end;
#end;
#if(err_count.equals(max_err));
#echo("You account is disabled\n");
#end;
echo("[TEST]Hello World!\n");
if(true);
echo("[TEST]True PASS\n");
end;
if(false);
else;
echo("[TEST]Else PASS\n");
end;
var(a,"[TEST]");
if(a.equals("[TEST]"));
echo("[TEST]Var PASS\n");
else;
echo("[TEST]Var Failed\n");
end;
var(n,0);
while(n.less(10));
echo("[TEST]While Loop:",n,"\n");
var(n,n.plus(1));
end;
